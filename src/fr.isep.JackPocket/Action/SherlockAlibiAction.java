package fr.isep.JackPocket.Action;

import fr.isep.JackPocket.AlibiCard;
import fr.isep.JackPocket.Game;
import fr.isep.JackPocket.Player;

import java.util.List;

public class SherlockAlibiAction extends ActionToken {
    /*+ pickAlibiCard(Player player, List<AlibiCards> cards) : void
    + moveSherlockToken(Game game, int numberCell) : void*/

    public void pickAlibiCard(Player player, List<AlibiCard> cards) {

    }

    public void moveSherlockToken(Game game, int numberCell) {

    }
}
