package fr.isep.JackPocket.Action;

import fr.isep.JackPocket.DetectiveToken;
import fr.isep.JackPocket.District;
import fr.isep.JackPocket.Orientation;

public class JackeRotateAction extends ActionToken {
    /*
    + moveToken(DetectiveToken detective, int numberCell) : void
+ rotate(District district, orientation Orientation) : void
     */
    public void moveToken(DetectiveToken detectiveToken, int numberCell){

    }
    public void rotate(District district, Orientation orientation){

    }
}
